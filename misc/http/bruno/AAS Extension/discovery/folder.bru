meta {
  name: discovery
  seq: 4
}

auth {
  mode: inherit
}

docs {
  # Returns a discovery document, the "self description".
  
  The self description is a list of AAS conformant AAS environment object per registered AAS server. It contains - next to AAS metadata describing each registered element - the EDC asset ID necessary to locate a desired element in the DSP catalog. Additionally to the catalog discovery, the self description retains the underlying AASs structure.
}
