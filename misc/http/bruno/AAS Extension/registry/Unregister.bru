meta {
  name: Unregister
  type: http
  seq: 2
}

delete {
  url: {{provider-api}}/registry?url={{registry-url}}
  body: none
  auth: inherit
}

params:query {
  url: {{registry-url}}
}

settings {
  encodeUrl: true
  timeout: 0
}
