meta {
  name: Unregister
  type: http
  seq: 2
}

delete {
  url: {{provider-api}}/service?url={{service-url}}
  body: none
  auth: inherit
}

params:query {
  url: {{service-url}}
}

settings {
  encodeUrl: true
  timeout: 0
}
