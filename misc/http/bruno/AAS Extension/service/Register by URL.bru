meta {
  name: Register by URL
  type: http
  seq: 1
}

post {
  url: {{provider-api}}/service
  body: json
  auth: inherit
}

body:json {
  {
    "url": "http://localhost:80/api/v3.0"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
