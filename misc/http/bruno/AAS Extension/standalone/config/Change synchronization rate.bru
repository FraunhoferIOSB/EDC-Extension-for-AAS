meta {
  name: Change synchronization rate
  type: http
  seq: 2
}

patch {
  url: {{standalone}}/config
  body: json
  auth: inherit
}

body:json {
  {
      "edc.aas.syncPeriod": 1
  }
}

settings {
  encodeUrl: true
}
