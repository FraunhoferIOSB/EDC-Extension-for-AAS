meta {
  name: create token
  type: http
  seq: 1
}

post {
  url: {{url}}/v1/auth/token/create
  body: json
  auth: inherit
}

headers {
  X-Vault-Token: <token>
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
