info:
  name: transfer status
  type: http
  seq: 5

http:
  method: GET
  url: "{{p2-management-api}}/transferprocesses/:transfer-id"
  params:
    - name: transfer-id
      value: e46c80f5-e921-45c0-ae7f-27efdc6a90a6
      type: path
  auth: inherit

runtime:
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: res.body['@id']
        method: jsonq
      variable:
        name: transfer-id
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
