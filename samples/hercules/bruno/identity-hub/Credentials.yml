info:
  name: Credentials
  type: http
  seq: 8

http:
  method: GET
  url: http://identity-hub:15151/api/identity/v1alpha/credentials
  auth:
    type: apikey
    key: x-api-key
    value: YWRtaW4=.s3cr3t
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
