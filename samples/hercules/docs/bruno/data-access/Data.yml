info:
  name: Data
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:9500/public
  headers:
    - name: Authorization
      value: "{{pull-secret}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
