info:
  name: Credentials
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:15151/api/identity/v1beta/credentials
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
