info:
  name: Credentials
  type: http
  seq: 8

http:
  method: GET
  url: http://identity-hub:15151/api/identity/v1alpha/credentials
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
